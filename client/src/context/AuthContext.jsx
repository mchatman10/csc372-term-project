import { createContext, useContext, useEffect, useState } from 'react'; import { API } from '../util/api'
const C = createContext(); export function AuthProvider({ children }) { const [user, setUser] = useState(null); const [loading, setLoading] = useState(true); useEffect(() => { (async () => { try { setUser(await API.me()) } catch { } setLoading(false) })() }, []); async function login(e, p) { const u = await API.login(e, p); setUser(u); return u } async function register(e, p, d) { const u = await API.register(e, p, d); setUser(u); return u } async function logout() { await API.logout(); setUser(null) } return <C.Provider value={{ user, loading, login, register, logout }}>{children}</C.Provider> } export function useAuth() { return useContext(C) }
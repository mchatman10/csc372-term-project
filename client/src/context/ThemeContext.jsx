import { createContext, useContext, useEffect, useState } from 'react'
const C = createContext(); export function ThemeProvider({ children }) { const [theme, setTheme] = useState('dark'); useEffect(() => { const s = localStorage.getItem('theme') || 'dark'; setTheme(s); document.documentElement.setAttribute('data-theme', s) }, []); function toggle() { const t = theme === 'dark' ? 'light' : 'dark'; setTheme(t); localStorage.setItem('theme', t); document.documentElement.setAttribute('data-theme', t) } return <C.Provider value={{ theme, toggle }}>{children}</C.Provider> } export function useTheme() { return useContext(C) }